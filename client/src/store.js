import { writable } from 'svelte/store'
export const version = writable(null);
export const lastCheck = writable(null);
export const allowReload = writable(null);
export const requestReload = writable(null);
export const user = writable(null);
export const server = writable(null);
export const select = writable(null);
export const ideaId = writable(null);
